<%= render partial: '/admin/simple_list/lists/form',
          locals: {
                  action: :edit,
                  form_options: {
                    method: :PUT,
                    class: 'my-form simple_list_form',
                    action: "#{model_url}/#{params[:id]}"
                  }
          }
%>
